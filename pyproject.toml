[project]
name = "terminal-image-viewer"
version = "0.1.0"
description = "Script to display images in terminal"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "hexstyle>=1.1.1",
    "pillow>=12.2.0",
]
